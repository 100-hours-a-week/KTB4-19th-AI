# 작업 로그 — 2026-09-16

## 오늘의 목표

`[AI] Main Agent Baseline` Issue의 범위와 커밋 단위를 확정하고, Agent Skeleton 구현을 시작한다.

## 확정한 설계

- Main Agent는 `/converse`의 공통 진입점, 상태, LLM 의도 분류, conditional routing, clarify 응답, 공통 응답 조립을 맡는다.
- `complaint`와 `knowledge`는 각 담당자가 feature handler를 구현한다. 두 기능은 LangGraph와 `AgentState`를 직접 의존하지 않는다.
- Agent는 LLM으로 `complaint`, `knowledge`, `clarify` 중 하나를 분류한다.
- 모델 호출 실패는 clarify로 숨기지 않고 upstream 오류로 처리한다.
- feature handler를 연결하기 전 E2E는 test double 기반 orchestration E2E로 한정한다.

## 구현 순서

- [ ] `feat(api): FastAPI 앱과 health endpoint 추가`
- [ ] `feat(contracts): 대화와 Agent 상태 계약 정의`
- [ ] `feat(agent): 라우팅 그래프와 feature 노드 접점 추가`
- [ ] `feat(agent): LLM 의도 분류 연결`
- [ ] `feat(agent): 명확화 응답 노드 추가`
- [ ] `feat(api): converse 라우터와 Agent 흐름 연결`

## 트러블슈팅

| 문제 | 원인 | 시도 | 해결/결정 | 재발 방지 |
| --- | --- | --- | --- | --- |
| 아직 없음 | - | - | - | - |

## 다음 작업

1. 첫 커밋의 완료 조건과 테스트를 작성한다.
2. 변경 전후 `ruff`와 `pytest`를 실행한다.
3. 막힘이 생기면 이 문서의 트러블슈팅 표를 먼저 갱신한다.

## 일일 작성 규칙

- 작업 시작 전: 오늘 완료할 커밋 하나와 완료 조건을 쓴다.
- 막힌 즉시: 문제, 재현 조건, 가설, 시도 결과를 기록한다.
- 작업 종료 전: 결정 사항, 미해결 항목, 다음 첫 작업을 한 줄로 남긴다.
